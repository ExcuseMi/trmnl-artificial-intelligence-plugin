# artificialanalysis.ai API key
AA_API_KEY=your_api_key_here

# Set to "true" to only allow requests from TRMNL's known IPs
ENABLE_IP_WHITELIST=false

# Host port to expose the backend on (default: 8000)
PORT=8707
